export interface MenuLabels {
  about: string;
  brand: string;
  contact: string;
  journey: string;
  menu: string;
  search: {
    compact: string;
    full: string;
    short: string;
    shortcut: string;
  };
  theme: {
    dark: string;
    light: string;
  };
}
